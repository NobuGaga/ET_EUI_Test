namespace ET
{
    /// <summary> Mono 层 Unit 类更新辅助类, 用来定义 Mono 层不同 Unit 的 Update</summary>
    public static class UpdateHelper
    {
        public static void UpdateBulletUnit(long unitId)
        {
            //TransformInfo info = TransformInfoComponent.Instance.Get(unitId);

            //var transformMonoComponent = TransformMonoComponent.Instance;
            //transformMonoComponent.SetLocalPos(unitId, info);
            //transformMonoComponent.SetLocalRotation(info.LocalRotation);
            //transformMonoComponent.UpdateScreenPosition();

            //var bulletUnitComponent = unit.BulletUnitComponent;
            //BulletMoveInfo info = bulletUnitComponent.Info;
            //self.ColliderMonoComponent.SetMoveDirection(info.MoveDirection);

            //self.ColliderMonoComponent.UpdateColliderCenter();
        }
    }
}